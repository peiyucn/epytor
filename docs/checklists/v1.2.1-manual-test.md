# v1.2.1 手测清单

> 测试包：`releases/epytor-vscode-1.2.1.vsix`（由 `main` 上那棵树构建，`main` 与 `dev` 树一致）
>
> **状态口径**：`[x]` **只表示 owner 明确确认过**（注明日期与测试包）。本版只有一条用户可感知改动。
>
> 上一版清单见 [`v1.2.0-manual-test.md`](v1.2.0-manual-test.md)（其中「『打开方式…』里显示为 `EPYTOR`（一段）」
> 一条未勾选——该写法在 VS Code 里做不到，原因见 [`../upstream-limits.md`](../upstream-limits.md) 第 6 项）。

## 1. 「打开方式…」里的编辑器名（本版唯一用户可感知改动）

* [x] **编辑器标题 `...` 菜单 → Reopen Editor With** 里本编辑器显示 `Markdown Editor - EPYTOR`（中文界面为 `Markdown 编辑器 - EPYTOR`）——owner 2026-09-13 明确确认（「两项都对，通过」）
* [x] 同菜单里的内置项不受影响：`Text Editor` / `Markdown Preview`——owner 同次确认

复现路径：`code --install-extension releases\epytor-vscode-1.2.1.vsix --force` → 重载窗口 → 打开任意 `.md` → 编辑器标题右侧 `...` → Reopen Editor With。

## 2. 无其它用户可感知改动

本版另外只含一个内部健壮性修复（吸顶缓存立即重建时撤掉待触发的防抖定时器，避免编辑器销毁后孤儿定时器抛未捕获异常）与文档/测试改动，均无用户可感知行为变化，故无需手测项。
